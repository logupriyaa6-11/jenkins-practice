## 4. Palindrom
Enter a word: level and store it in $InputWord

checks for non empty string 

declare a string to store the reversed word -> $reverseWord

run a for loop from back of the string and store it in $reverse string 

check whether they are equal, if equal function returns true

**output**
The word is a palindrome.